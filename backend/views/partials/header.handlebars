<header>
    <h2>Seleções</h2>

    <nav>
        <a href="/">Inicio</a>
    </nav>

    
    {{#if usuario}}
        <p>Olá, {{usuario.nome}}</p>

    {{else}}
        <a href="/login">Entrar</a>
    {{/if}}
</header>